# Sets

## A set has unique values

```python
set1 = set(["a", "b", "c", "a"])
```

```
{'a', 'c', 'b'}
```

## You can subtract to find the differences

```python
set1 = set(["a", "b", "c"])
set2 = set(["a", "x"])

set1 - set2
```

```
{'b', 'c'}
```
